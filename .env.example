# Public values only. Never use service_role, sb_secret_, or database passwords here.
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=YOUR_PUBLIC_PUBLISHABLE_KEY
# Only for explicitly running the legacy offline app locally. Never set on Render.
VITE_LOCAL_ONLY=false
